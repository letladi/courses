// deletes the characters between start and end (inclusive)
int splice(char s[], int start, int end);
// returns the index of the next, the next characters index after \n
int next_line_start_index(char s[], int start);

// remove comments from program code
void remove_comments(char prog[], int i);

void copy_str(char dest[], char src[]);

int strLen(char s[]);