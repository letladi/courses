/**
 * The only arithmetic operations that can be performed on pointers are 
 * addition and subtraction ❌ Pointers cannot be added; you can add 
 * an integer to a pointer, but not a pointer to another pointer.
 * On the other hand pointers can be subtracted. The correct code is;
 * middle  = (low + high) / 2;
 */
