/*
   the second line must have two variables (separated by a colon) and not semicolons
   the second argument does not have a type
   ❌
   The types of both base and height must not be specified
   using the 'product' in the function is an error
   the names of the parameters and those on the second line must be the same
*/

// double average(a, b)
// double a, b;
// {
//    return (a + b) / 2;
// }

double triangle_area(base, height)
double base;
{
   base = base * height;
   return base / 2;
}


int main(void)
{
   return 0;
}