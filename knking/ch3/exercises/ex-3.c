/*
a) the second string will only read an integer that is preceded by a space character ❌ 
   [the two are equivalent]
b) the first string will accept input of 3 integers separated by dashes,
   the second string will accept integers in which the first
   and second numbers are succeeded by spaces.  ❌ 
   [the two are equivalent]
c) the second format string will require a space after a number is entered ✔️ 
   [as well as entering some other input for the second string]
d) the first number requires two numbers with a comma between them.
   In the second input, a space after the comma is required ❌ [the two input strings are equivalent]
*/